-# children, parent_set
- children.sort{|a,b| a[:name] <=> b[:name]}.each do |child|
  - new_set = parent_set + [child[:id]]
  %tr{:id=>set_id(new_set), :class=>parent_set_class(parent_set)}
    %td
      #{child[:name]}
  = render :partial=>"providers/redhat/children", :locals=>{:children=>child[:children], :parent_set=>new_set} if !child[:children].empty?
  = render :partial=>"providers/redhat/repo", :collection=>child[:repos].sort{|a,b| a.name <=> b.name}, :as=>:repo,
            :locals=>{:parent_set=>new_set} if !child[:repos].empty?
