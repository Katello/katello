This is Headpin command line client.

Usage: headpin [options]

HACKING

Run unit tests (install python-mock - available in Fedora 17+)

  nosetests

Compile a test RPM:

  tito build --rpm --test --rpmbuild-options=--nodeps
