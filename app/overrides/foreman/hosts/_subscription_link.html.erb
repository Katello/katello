<%= link_to(
        _('Content'),
        "/content_hosts/#{@host.content_host.uuid}/info",
        :title => _("Host content and subscription details"),
        :class => 'btn btn-default') if @host.content_host
%>
