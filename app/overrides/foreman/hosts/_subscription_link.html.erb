<% system = Katello::System.find_by_host_id(@host.id) %>
<%= link_to(
        _('Content'),
        "/content_hosts/#{system.uuid}/info",
        :title => _("Host content and subscription details"),
        :class => 'btn btn-default') if system
%>
