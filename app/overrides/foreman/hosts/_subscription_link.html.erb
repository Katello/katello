
<% content_host = Katello::System.where(:host_id => @host.id).first %>
<%= link_to(
        _('Content'),
        "/content_hosts/#{content_host.uuid}/info",
        :title => _("Host content and subscription details"),
        :class => 'btn btn-default') if content_host
%>
