- for org in user.allowed_organizations
  %li.menu-item
    = render :partial=>"common/fav", :locals => {:org=>org, :current_user=>current_user}
    = link_to org.name, set_org_user_session_path(:org_id=>org.id), :method=>"POST", :class=>"selected dropdown-menu-item-link"
