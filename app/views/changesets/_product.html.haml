%ul
  %li
    .slide_link{:id=>packages_cs_bc_id(changeset, product)}
      %span #{_("Packages")}

  %li
    .slide_link{:id=>errata_cs_bc_id(changeset, product)}
      %span #{_("Errata")}

  %li
    .slide_link{:id=>repos_cs_bc_id(changeset, product)}
      %span #{_("Repositories")}