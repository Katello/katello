- if flash[:error]
  :javascript
    $(document).ready(function(){
      notices.displayNotice("error", '#{ escape_javascript(flash[:error]) }');
    });

- if flash[:warning]
  :javascript
    $(document).ready(function(){
      notices.displayNotice("warning", '#{ escape_javascript(flash[:warning]) }');
    });

- if flash[:success]
  :javascript
    $(document).ready(function(){
      notices.displayNotice("success", '#{ escape_javascript(flash[:success]) }');
    });

- if flash[:message]
  :javascript
    $(document).ready(function(){
      notices.displayNotice("", '#{ escape_javascript(flash[:message]) }');
    });

- if flash[:notice]
  :javascript
    $(document).ready(function(){
      notices.displayNotice("", '#{ escape_javascript(flash[:notice]) }');
    });
