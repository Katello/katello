.ca.light_text.versioning
  - if current_user
    #{link_to (_("%{app_name} Version: %{version}") % {:app_name => release_name, :version => AppConfig.katello_version}), about_path}
  - else
    #{_("%{app_name} Version: %{version}") % {:app_name => release_name, :version => AppConfig.katello_version}}
