#panel-frame
  #panel.closed.panel{:class => custom_panel ? 'panel-custom' : '' }
    .data
      .spinner
        = image_tag ("spinner.gif")
      .panel-content
      %br.clear

#subpanel-frame
  #subpanel.panel.subpanel.closed
    .data
      .spinner
        = image_tag ("spinner.gif")
      .panel-content
      %br.clear