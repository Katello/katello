= content_for :title do
  #{@group.name}

= content_for :remove_item do
  - if @group.deletable?
    = link_to(_("Remove System Group"), {:action => "destroy", :controller => "system_groups", :id => @group.id},
      {:class => "remove_item"})

= content_for :navigation do
  = render_menu(1..2, system_groups_navigation)
