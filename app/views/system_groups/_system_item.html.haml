
%tr{'data-id'=>system.id, :class=> cycle("", "alt")}
  %td
    = system.name
    - if @group.editable? && !@group.locked
      %a.fr.st_button.remove_system{'data-id'=>system.id}
        = _("Remove")
