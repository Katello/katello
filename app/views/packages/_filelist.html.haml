= content_for :title do
  = @package.nvrea

= content_for :navigation do
  = render_navigation(:items => promotion_packages_navigation, :expand_all => true, :level => 1)

= content_for :content do
  -#%h2 
    -#= _("Filelist")
  
  file 1
  %br
  file 2