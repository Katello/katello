#
# WARNING: THIS CONFIGURATION WAS GENERATED BY KATELLO-CONFIGURE TOOL,
# CHANGES WILL LIKELY BE OVERWRITTEN.
#

[server]
host = localhost.localdomain
port = 443
scheme = https
path = <%= "/#{scope.lookupvar('katello::params::deployment_url')}" %>

[interface]
grep_friendly = false

[shell]
nohistory = false
prompt = <%= "#{scope.lookupvar('katello::params::deployment_url')}>" %>
