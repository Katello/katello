admin:
  login:       admin
  password:    nimda
  mail:        admin@wayne.ent.com
  remote_id:   admin
  preferences: <%= HashWithIndifferentAccess.new({}).to_yaml.inspect %>

hidden:
  login:       hidden
  password:    nimda
  mail:        hidden@wayne.ent.com
  remote_id:   hidden
  hidden:      true
  preferences:  <%= HashWithIndifferentAccess.new({}).to_yaml.inspect %>

no_perms_user:
  login:       Alfred
  password:    <%= Password.update('Alfred') %>
  mail:        alfred@wayne.ent.com
  preferences: <%= HashWithIndifferentAccess.new({}).to_yaml.inspect %>

disabled_user:
  login:       disabled_user
  password:    disabled_user_password
  mail:        disabled@user.com
  disabled:    true
  remote_id:   disabled
  preferences: <%= HashWithIndifferentAccess.new({}).to_yaml.inspect %>
