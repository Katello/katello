administrator_admin:
  role:  administrator
  user:  admin

admin_own_role_admin:
  role:   admin_own_role
  user:   admin

no_perms_user_own_role:
  role:   no_perms_own_role
  user:   no_perms_user

no_perms_user_own_role:
  role:   no_perms_own_role
  user:   no_perms_user

authorized_user_own_role:
  role:   authorized_own_role
  user:   authorized

unauthorized_user_own_role:
  role:   unauthorized_own_role
  user:   unauthorized

hidden_own_role:
  role:   hidden_own_role
  user:   hidden