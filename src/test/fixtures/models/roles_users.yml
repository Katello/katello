administrator_admin:
  role:  administrator
  user:  admin

admin_own_role_admin:
  role:   admin_own_role
  user:   admin

no_perms_user_own_role:
  role:   no_perms_own_role
  user:   no_perms_user

hidden_own_role:
  role:   hidden_own_role
  user:   hidden