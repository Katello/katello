library_fedora:
  environment:  library
  product:      fedora

dev_fedora:
  environment:  dev
  product:      fedora

library_redhat:
  environment:  library
  product:      redhat

dev_redhat:
  environment:  dev
  product:      redhat
