.block.tall{:id => "#{name}_#{item.send(accessor)}", "data-ajax_url" => "#{url_for(:controller=>controller.controller_name)}/#{item.send(accessor)}", "data-ajax_panelpage" => :systems}
  %div{:class => "column_1"}
    %span
      = "#{truncate(item.name, :length => 50)} (#{item.systems.count})"
