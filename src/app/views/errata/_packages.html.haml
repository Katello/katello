
%h2
  = @errata.title

%nav.subnav
  = render_navigation(promotion_errata_navigation, :expand_all => true, :level => 1)


%h2
  = _("Packages")
.clear
  &nbsp;
  
- @errata.pkglist.each do |pkg|
  .li.grid_6
    = pkg
