#{_("A request has been received by Katello server '%s' to retrieve the logins for email '%s'. The following is a list of those logins:") % [root_url, @email]}
- @users.each do |user|
  = user.username

